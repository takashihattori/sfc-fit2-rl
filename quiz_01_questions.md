# 第1回小テスト：プログラムとは（問題定義）

- **テスト構成**: 全5問（各グループからランダムに1問出題、各1点 / 5点満点）
- **シャッフル**: 選択肢のシャッフル有効
- **生成ファイル**: [quiz_01_program_intro.zip](file:///Users/hattori/SFC-CNS%20Dropbox/Hattori%20Takashi/Documents/%E6%83%85%E5%A0%B1%E6%8A%80%E8%A1%93%E7%A7%91%E7%9B%AE/%E6%95%99%E6%9D%90%E4%BD%9C%E6%88%90/fit2-text/quiz_01_program_intro.zip)

---

## 【グループ1】コンピュータの構成と動作（CPUと記憶装置）
> ※ 以下の問題からランダムに1問出題（配点：1点）

### 問題 1-1（単一選択）
コンピュータの主記憶（メインメモリ）と補助記憶（SSD/HDD）の違いに関する説明として、**正しいもの**を1つ選んでください。

- [ ] A. 主記憶は不揮発性であり、電源を切ってもデータが消えることはない。
- [*] B. プログラム実行時、CPUは補助記憶から直接ではなく、主記憶に読み込まれた命令を1個ずつ取り出して実行する。
- [ ] C. 補助記憶は主記憶に比べてアクセス速度が数十倍から数千倍高速である。
- [ ] D. 主記憶にはOSや写真・動画ファイルが永続的に保存される。

> **【正解】B**  
> **【解説】** 主記憶（メインメモリ）は高速ですが揮発性（電源OFFで消去）です。プログラムを実行する際は、まず補助記憶から主記憶にプログラムがコピーされ、CPUは主記憶から命令を読み込んで実行します。

---

### 問題 1-2（単一選択）
コンピュータの頭脳であるCPU（中央演算処理装置）を構成する主な2つの機能部として、**適切な組み合わせ**を1つ選んでください。

- [ ] A. 入力部 と 出力部
- [*] B. 制御部 と 演算部
- [ ] C. 主記憶部 と 補助記憶部
- [ ] D. 翻訳部 と 実行部

> **【正解】B**  
> **【解説】** CPUは主に、計算や値の比較を行う「演算部（ALU）」と、メモリから命令を取り出して解読・指示を送る「制御部」から構成されています。

---

### 問題 1-3（単一選択）
CPUが直接理解して実行できる「機械語（マシン語）」に関する説明として、**最も適切なもの**を1つ選んでください。

- [ ] A. 人間が日常的に話す英語に近い単語で書かれたプログラムである。
- [ ] B. 世界中のすべてのCPUで共通規格となっており、どのコンピュータでもそのまま動く。
- [*] C. `0` と `1` のビット列（数値）で表現された命令であり、CPUの種類（アーキテクチャ）ごとに仕様が異なる。
- [ ] D. Pythonなどのスクリプト言語のコードそのもののことである。

> **【正解】C**  
> **【解説】** 機械語は `0` と `1` の並びで表現されており、CPUの種類（x86_64、ARMなど）によって仕様が異なります。人間にとって読み書きが難しいため、Pythonなどの高水準言語が生まれました。

---

## 【グループ2】プログラミング言語
> ※ 以下の問題からランダムに1問出題（配点：1点）

### 問題 2-1（単一選択）
高校「情報I」でも扱われた、アルゴリズムを組み立てる**基本の3つの制御構造**の組み合わせとして、正しいものを1つ選んでください。

- [ ] A. 入力、計算、出力
- [*] B. 順次、分岐、反復
- [ ] C. 定義、代入、参照
- [ ] D. コンパイル、実行、デバッグ

> **【正解】B**  
> **【解説】** あらゆるプログラムは「順次（上から順に実行）」「分岐（条件によって分ける）」「反復（同じ処理を繰り返す）」という3つの基本構造の組み合わせで記述できます。

---

### 問題 2-2（単一選択）
人間が記述する「高水準言語（プログラミング言語）」と、コンピュータが処理する「機械語」の関係についての説明として、**最も適切なもの**を1つ選んでください。

- [ ] A. CPUは高水準言語で書かれた英単語を直接解読してそのまま実行できる。
- [ ] B. 高水準言語は機械語よりもコンピュータが高速に処理できるように作られたものである。
- [*] C. 人間が書いた高水準言語のコード（ソースコード）は、翻訳ソフトウェア（インタプリタやコンパイラ）によって機械語に変換されてから実行される。
- [ ] D. 現在のコンピュータでは機械語は一切使われておらず、高水準言語のみで動作している。

> **【正解】C**  
> **【解説】** CPUが解釈できるのは `0` と `1` の機械語のみです。人間が理解しやすい英単語や数式で書いたソースコードは、翻訳ソフトウェアによって機械語に変換されて初めて実行されます。

---

### 問題 2-3（単一選択）
「世界共通のプログラミング言語が1つだけではなく、多種多様な言語が存在している理由」に関する説明として、**最も適切なもの**を1つ選んでください。

- [ ] A. 時代とともに古い言語の文法がすべて使えなくなるため、毎年新しい言語を作り直しているから。
- [ ] B. コンピュータのメーカー各社が自社製品以外でプログラムを動かせないように囲い込んでいるから。
- [*] C. ハードウェアの高速制御、Webブラウザ上の動作、データ分析など、解決したい目的や用途によって言語の得意分野が異なるから。
- [ ] D. 英語圏、アジア圏、ヨーロッパ圏など、話されている母国語ごとにプログラミング言語が分かれているから。

> **【正解】C**  
> **【解説】** 大工道具にノコギリや金づちがあるように、プログラミング言語にも「高速動作が得意（C言語等）」「Webブラウザで動く（JavaScript等）」「データ分析・機械学習が手軽（Python等）」といった適材適所のトレードオフがあります。

---

## 【グループ3】Pythonの特徴と実行形態
> ※ 以下の問題からランダムに1問出題（配点：1点）

### 問題 3-1（単一選択）
Pythonが**「インタプリタ型言語」**であることのメリット・特徴として、最も適切なものを1つ選んでください。

- [ ] A. 実行前にプログラム全体を一括して機械語に変換する長い待ち時間が必要である。
- [*] B. プログラムを1行ずつ解釈しながら対話的に実行できるため、試行錯誤やデータの確認がしやすい。
- [ ] C. プログラムにエラーが含まれていても、エラーを自動的に修正して実行を継続してくれる。
- [ ] D. 他のどのプログラミング言語よりもコンピュータの実行速度（計算速度）が極限まで速い。

> **【正解】B**  
> **【解説】** インタプリタ型言語であるPythonは、コードを1行ずつ解釈しながら対話的・即時的に実行できます。これにより、初学者の学習や試行錯誤、データ分析に非常に適しています。

---

### 問題 3-2（単一選択）
Python言語の特徴に関する説明として、**誤っているもの**を1つ選んでください。

- [ ] A. シンプルで読みやすい文法設計になっており、コードの可読性が高い。
- [ ] B. 数値計算、データ分析、機械学習などの強力なライブラリ群が世界中で豊富に提供されている。
- [ ] C. インタプリタ型言語であり、コードを1行ずつ解釈しながら対話的に実行して結果を確認できる。
- [*] D. コンピュータのCPUが翻訳ソフトウェア（インタプリタ等）を通さずに直接理解・実行できる。

> **【正解】D**  
> **【解説】** CPUが翻訳なしで直接実行できるのは `0` と `1` で書かれた「機械語」のみです。Pythonは人間が理解しやすい高水準言語であるため、実行にはインタプリタによる機械語への翻訳が必要です。

---

### 問題 3-3（単一選択）
Pythonプログラムを実行する「対話型シェル（REPL）」と「スクリプトファイル（.pyファイル）」の使い分けに関する説明として、**最も適切なもの**を1つ選んでください。

- [ ] A. 対話型シェルに一度入力したコードは自動的にファイルとして永久保存されるため、本格的な開発はすべて対話型シェルで行う。
- [*] B. 関数の挙動確認や簡単な計算には対話型シェルが便利であり、まとまった一連の処理を作成する場合はスクリプトファイルに保存して実行する。
- [ ] C. スクリプトファイルには `print()` 関数を書くことができず、計算処理しか記述できない。
- [ ] D. 対話型シェルとスクリプトファイルでは、使用できるPythonの文法が完全に異なる。

> **【正解】B**  
> **【解説】** `>>> ` が表示される対話型シェルはその場での簡易確認や実験に適しており、まとまったプログラムはエディタ等で `.py` ファイルに記述・保存して実行します。

---

## 【グループ4】基本文法と変数・演算子
> ※ 以下の問題からランダムに1問出題（配点：1点）

### 問題 4-1（単一選択）
Pythonにおけるプログラミングの「代入文」`x = 10` の意味として、**最も適切なもの**を1つ選んでください。

- [ ] A. 数学の方程式と同じく、「x と 10 が等しい」という状態を表している。
- [*] B. 右辺の値（10）を、左辺の変数（x）という名前の記憶領域に格納する。
- [ ] C. 変数 x の中身を空にして、新しく 10 という名前の変数を作る。
- [ ] D. 画面に「x = 10」という文字列を出力する。

> **【正解】B**  
> **【解説】** プログラミングにおける `=` は等号ではなく「代入演算子」です。「右辺の計算結果や値を左辺の変数に入れる」という操作を意味します。

---

### 問題 4-2（単一選択）
以下のPythonプログラムを実行したとき、画面に出力される値として**正しいもの**を1つ選んでください。

```python
count = 5
count = count + 3
count = count * 2
print(count)
```

- [ ] A. `5`
- [ ] B. `8`
- [*] C. `16`
- [ ] D. `11`

> **【正解】C**  
> **【解説】** 最初 `count` に 5 が代入されます。次に `5 + 3` の結果である 8 が上書き代入され、最後に `8 * 2` の結果である 16 が代入されて出力されます。

---

### 問題 4-3（単一選択）
Pythonで**変数名**として使用したとき、文法エラーにならず**正しく使える名前**を1つ選んでください。

- [ ] A. `2nd_total` （先頭が数字）
- [*] B. `user_score` （アルファベットとアンダースコア）
- [ ] C. `total score` （途中に半角スペースを含む）
- [ ] D. `user-name` （引き算記号のハイフンを含む）

> **【正解】B**  
> **【解説】** 変数名に使える文字はアルファベット・数字・アンダースコア `_` です。ただし先頭の文字に数字を使うことはできません（Aは不可）。またスペース（C）や演算子記号（D）も含めることはできません。

---

### 問題 4-4（単一選択）
Pythonプログラムにおける「空白（スペース）」や「コメント」のルールに関する説明として、**誤っているもの**を1つ選んでください。

- [ ] A. `#` 記号からその行の末尾まではコメントとなり、コンピュータは実行時にこれを無視する。
- [ ] B. プログラムコードの途中に全角スペース（日本語入力の空白）が紛れ込むと、文法エラー（`SyntaxError`）の原因になる。
- [ ] C. `+` や `=` などの演算子の前後に半角スペースを空けて記述しても、文法エラーにはならず正しく実行される。
- [*] D. `#` で始まるコメントはプログラムの最初の1行目にしか書くことができず、コードと同じ行の右側に書くことはできない。

> **【正解】D**  
> **【解説】** コメント記号 `#` は行の先頭だけでなく、コードの途中（行末）にも書くことができます（例：`count = 5  # 初期値を設定`）。

---

## 【グループ5】関数とモジュール
> ※ 以下の問題からランダムに1問出題（配点：1点）

### 問題 5-1（単一選択）
Pythonで新しく独自の関数を定義するときに使用するキーワードとして、**正しいもの**を1つ選んでください。

- [ ] A. `function`
- [*] B. `def`
- [ ] C. `define`
- [ ] D. `func`

> **【正解】B**  
> **【解説】** Pythonでの関数定義は `def 関数名(引数):` という書式で記述します。

---

### 問題 5-2（単一選択）
以下のPythonプログラムを実行したときに出力される結果として、**正しいもの**を1つ選んでください。

```python
def calc(x):
    return x * 3 + 1

ans = calc(4)
print(ans)
```

- [ ] A. `7`
- [*] B. `13`
- [ ] C. `12`
- [ ] D. `calc(4)`

> **【正解】B**  
> **【解説】** 引数 `x` に `4` が渡され、関数内で `4 * 3 + 1 = 13` が計算されて `return` で返されます。それが変数 `ans` に代入されて表示されます。

---

### 問題 5-3（単一選択）
Pythonで標準モジュール `math` を読み込み、そこに含まれる円周率の値 `pi` を使いたいときの記述方法として、**最も適切なもの**を1つ選んでください。

- [ ] A.
  ```python
  load math
  print(pi)
  ```
- [*] B.
  ```python
  import math
  print(math.pi)
  ```
- [ ] C.
  ```python
  using math
  print(math->pi)
  ```
- [ ] D.
  ```python
  include <math.h>
  print(math:pi)
  ```

> **【正解】B**  
> **【解説】** Pythonではモジュールを取り込むために `import モジュール名` を使い、その中の関数や定数には `モジュール名.要素名` とドットで繋いでアクセスします。

---

### 問題 5-4（単一選択）
組み込み関数 `len()` と `max()` の使い方に関する説明として、**正しいもの**を1つ選んでください。

- [ ] A. `len("Python")` は文字列を大文字に変換する。
- [*] B. `len("Keio SFC")` は文字列の文字数を返し、`max(3, 5, 1)` は与えられた引数の中で最大値を返す。
- [ ] C. `max()` 関数は引数を必ず2つしか指定できない。
- [ ] D. `len()` 関数は変数のメモリ使用量（バイト数）を調べる関数である。

> **【正解】B**  
> **【解説】** `len()` は文字列の長さ（文字数）を返し、`max()` は渡された複数の引数の中から最も大きい値を返します。
