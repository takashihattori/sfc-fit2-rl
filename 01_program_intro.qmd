# プログラムとは

## 本章のねらい
高校の必履修科目「情報I」で学んだプログラミングの基本を振り返りつつ、「情報基礎1」のコンピュータ・アーキテクチャの知識をもとに、CPUや記憶装置（主記憶・補助記憶）がどのように連携してプログラムを実行しているのかを理解します。また、機械語と高水準言語の違いやPython言語の特徴を学んだ上で、公式標準インストーラを用いたPython開発環境の導入、ターミナルやVS Codeを使ったプログラムの実行方法を習得します。

---

## 高校「情報I」のプログラミングの復習

2022年度から高等学校で必修化された「情報I」では、プログラミングやアルゴリズムの基礎、データの活用について学習してきました。大学でのプログラミング学習に入る前に、高校でどのようなプログラミングを体験したか振り返ってみましょう。

高校「情報I」で扱ったアルゴリズムは、基本的に以下の3つの制御構造の組み合わせで組み立てられています：

1. **順次（じゅんじ）**: 上から下へ順番に命令を1つずつ実行する。
2. **分岐（ぶんき）**: 条件が成り立つかどうかに応じて実行する処理を切り替える（例：`if` 文）。
3. **反復（はんぷく）**: 条件を満たしている間、同じ処理を繰り返す（例：`for` 文、`while` 文）。

これらに加えて、リストなどの配列、探索（線形探索・二分探索）やソートなどのアルゴリズム、シミュレーションモデルを学んだ受講生も多いはずです。

::: {.callout-note icon=false}
### 練習問題 01-01：高校でのプログラミング体験
高校の授業や部活動、独学などでプログラミングをした経験について、以下の点を確認してみましょう。

1. どのプログラミング言語を使いましたか？（例：Python、JavaScript、VBA、Scratch などのビジュアル言語）
2. どのようなプログラムや作品を作りましたか？（例：おみくじ、自動販売機のシミュレーション、計算ドリル、タートルグラフィックス、ゲームなど）
:::

---

## コンピュータの動作

プログラムがどのように動くかを理解するには、コンピュータ本体（ハードウェア）の仕組みを知ることが不可欠です。

::: {.callout-note icon=false}
### 練習問題 01-02：コンピュータを構成する主要な5つの機能
春学期の「情報基礎1」で学んだコンピュータ・アーキテクチャをもとに、コンピュータを構成するための**主要な5つの機能（コンピュータの5大機能・5大装置）**を答えてください。
:::

現代のコンピュータは、**演算**と**制御**を担う **CPU** と、プログラムやデータを記憶する **記憶装置** を中心に動作しています。

```{=html}
<svg viewBox="0 0 720 320" width="100%" height="320" xmlns="http://www.w3.org/2000/svg" style="font-family: sans-serif; background: #fafafa; border-radius: 8px; border: 1px solid #e0e0e0; margin: 1.5em 0;">
  <!-- Background Boxes -->
  <rect x="20" y="50" width="120" height="80" rx="6" fill="#e3f2fd" stroke="#1976d2" stroke-width="2"/>
  <text x="80" y="85" text-anchor="middle" font-size="15" font-weight="bold" fill="#0d47a1">入力装置</text>
  <text x="80" y="108" text-anchor="middle" font-size="12" fill="#555">キーボード等</text>

  <!-- CPU Box -->
  <rect x="200" y="20" width="220" height="140" rx="8" fill="#ede7f6" stroke="#512da8" stroke-width="2"/>
  <text x="310" y="48" text-anchor="middle" font-size="16" font-weight="bold" fill="#311b92">CPU（中央演算処理装置）</text>
  <rect x="220" y="65" width="85" height="75" rx="4" fill="#fff" stroke="#7e57c2" stroke-width="1.5"/>
  <text x="262" y="98" text-anchor="middle" font-size="13" font-weight="bold" fill="#311b92">制御部</text>
  <text x="262" y="120" text-anchor="middle" font-size="11" fill="#666">命令の解読</text>
  <rect x="315" y="65" width="85" height="75" rx="4" fill="#fff" stroke="#7e57c2" stroke-width="1.5"/>
  <text x="357" y="98" text-anchor="middle" font-size="13" font-weight="bold" fill="#311b92">演算部</text>
  <text x="357" y="120" text-anchor="middle" font-size="11" fill="#666">計算の実行</text>

  <!-- Output Box -->
  <rect x="480" y="50" width="120" height="80" rx="6" fill="#e8f5e9" stroke="#388e3c" stroke-width="2"/>
  <text x="540" y="85" text-anchor="middle" font-size="15" font-weight="bold" fill="#1b5e20">出力装置</text>
  <text x="540" y="108" text-anchor="middle" font-size="12" fill="#555">画面・ファイル</text>

  <!-- Main Memory -->
  <rect x="200" y="210" width="220" height="85" rx="6" fill="#fff3e0" stroke="#f57c00" stroke-width="2"/>
  <text x="310" y="242" text-anchor="middle" font-size="15" font-weight="bold" fill="#e65100">主記憶（メインメモリ）</text>
  <text x="310" y="265" text-anchor="middle" font-size="12" fill="#bf360c">高速・揮発性（電源OFFで消去）</text>
  <text x="310" y="283" text-anchor="middle" font-size="11" fill="#777">実行中のプログラム・データを保持</text>

  <!-- Secondary Storage -->
  <rect x="480" y="210" width="200" height="85" rx="6" fill="#fce4ec" stroke="#c2185b" stroke-width="2"/>
  <text x="580" y="242" text-anchor="middle" font-size="15" font-weight="bold" fill="#880e4f">補助記憶（SSD / HDD）</text>
  <text x="580" y="265" text-anchor="middle" font-size="12" fill="#ad1457">大容量・不揮発性（永続保持）</text>
  <text x="580" y="283" text-anchor="middle" font-size="11" fill="#777">ファイルやOSを保存</text>

  <!-- Arrows -->
  <defs>
    <marker id="arrow" viewBox="0 0 10 10" refX="6" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M 0 1 L 8 5 L 0 9 z" fill="#444" />
    </marker>
    <marker id="arrow-blue" viewBox="0 0 10 10" refX="6" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M 0 1 L 8 5 L 0 9 z" fill="#1976d2" />
    </marker>
  </defs>

  <!-- Input to CPU -->
  <line x1="140" y1="90" x2="192" y2="90" stroke="#444" stroke-width="2" marker-end="url(#arrow)"/>
  <!-- CPU to Output -->
  <line x1="420" y1="90" x2="472" y2="90" stroke="#444" stroke-width="2" marker-end="url(#arrow)"/>

  <!-- CPU and Main Memory (Read/Write) -->
  <line x1="280" y1="202" x2="280" y2="168" stroke="#d84315" stroke-width="2" marker-end="url(#arrow)"/>
  <line x1="340" y1="168" x2="340" y2="202" stroke="#d84315" stroke-width="2" marker-end="url(#arrow)"/>
  <text x="310" y="190" text-anchor="middle" font-size="11" font-weight="bold" fill="#d84315">命令読込/書込</text>

  <!-- Main Memory and Storage -->
  <line x1="420" y1="252" x2="472" y2="252" stroke="#444" stroke-width="2" stroke-dasharray="4" marker-end="url(#arrow)"/>
  <line x1="472" y1="262" x2="428" y2="262" stroke="#444" stroke-width="2" stroke-dasharray="4" marker-end="url(#arrow)"/>
  <text x="450" y="242" text-anchor="middle" font-size="10" fill="#666">ロード</text>
</svg>
```

### CPU（中央演算処理装置）
CPU（Central Processing Unit）は、コンピュータの「頭脳」にあたる最も中心的なハードウェアです。

- **演算と制御を行う**:
  - **演算部（ALU: Arithmetic Logic Unit）**: 足し算や掛け算などの数値計算、値の比較などの論理演算を行います。
  - **制御部**: プログラムの命令を順番に取り出し、解読して各ハードウェアへ指示を送ります。
- **命令を記憶装置から1個ずつ読み込んで実行する**:
  CPUは独自の意志で動いているわけではありません。「命令の取り出し（Fetch）→ 命令の解読（Decode）→ 命令の実行（Execute）→ 結果の書き戻し（Write Back）」という基本サイクルを毎秒数十億回（GHzの単位）繰り返しています。
- **機械語命令の例**:
  CPUが直接理解できるのは、`0` と `1` のビット列で表現された**機械語（マシン語）**です。
  例えば、CPU内部の記憶場所（レジスタ）に対して以下のような極めて細かい命令が与えられます：
  ```
  LOAD  R1, [100]   ; メモリ番地100のデータをレジスタ1に読み込む
  LOAD  R2, [104]   ; メモリ番地104のデータをレジスタ2に読み込む
  ADD   R1, R2      ; レジスタ1とレジスタ2の値を加算し、結果をレジスタ1に保存
  STORE [108], R1   ; 結果をメモリ番地108に書き戻す
  ```
  （※ 人間が読めるようにアセンブリ言語で表現していますが、実際にはこれらが `01101001...` のようなビットパターンになっています）

### 記憶装置（主記憶と補助記憶）
コンピュータには、速度や用途の異なる2種類の記憶装置が存在します。

| 項目 | 主記憶（メインメモリ） | 補助記憶（ストレージ） |
| :--- | :--- | :--- |
| **主な役割** | CPUが今まさに実行しているプログラムや作業データを保持する | OS、アプリケーション、各種ファイルを永続的に保存する |
| **速度** | 非常に高速（ナノ秒単位） | 比較的低速（主記憶に比べて数百〜数千倍遅い） |
| **容量** | 8GB 〜 64GB 程度 | 256GB 〜 数TB（大容量） |
| **揮発性** | **揮発性**（電源を切ると内容はすべて消去される） | **不揮発性**（電源を切ってもデータは保持される） |
| **装置** | DRAM (Dynamic Random Access Memory) | HDD (Hard Disk Drive), SSD (Solid State Drive) |

- **CPUが命令を読み込むのは主記憶から**:
  補助記憶装置は読み書きが遅いため、CPUが補助記憶から直接命令を1個ずつ読み込んで実行することはできません。プログラムを実行するときは、**まず補助記憶（SSD等）に保存されているプログラムファイルが主記憶（メモリ）上にコピー（ロード）され、CPUはその主記憶から命令を1個ずつ読み込んで実行します**。

---

## プログラムの基本的概念

### プログラムとは何か
プログラムとは、**「コンピュータに実行させたい処理の手順（命令の並び）を、一定のルールに従って書き表したもの」**です。料理でいう「レシピ」や、音楽でいう「楽譜」に例えられます。

### 機械語と高水準言語
- **機械語**:
  CPUが解釈できる唯一の言語。`0` と `1` の数字の羅列であり、人間が直接読み書きしたり間違いを探したりするのは極めて困難です。また、CPUの種類（Intel/AMDのx86_64、Apple SiliconなどのARM）によって機械語の仕様が異なります。
- **高水準言語（プログラミング言語）**:
  人間が理解しやすい英単語や数式に近い形で記述できるように工夫された言語です（例：Python、C言語、Java、JavaScriptなど）。
  人間が高水準言語で書いたプログラム（**ソースコード**）は、コンパイラやインタプリタといった翻訳ソフトウェアによって機械語へと変換されてから実行されます。

### なぜいろいろな言語があるのか
「世界共通のプログラミング言語が1つだけあれば便利なのでは？」と思うかもしれません。しかし、言語にはそれぞれ得意な用途や設計思想のトレードオフがあります：

- **ハードウェアを極限まで速く動かしたい**: C言語、C++、Rust（メモリやハードウェアを直接制御できるが、記述が難しく開発に手間がかかる）
- **WebサイトやWebアプリを動かしたい**: JavaScript、TypeScript（ブラウザ上で標準動作する）
- **大規模な基幹系エンタープライズシステム**: Java、C#（型安全性が高く、多人数での保守性に優れる）
- **統計解析や機械学習、科学技術計算を手軽に行いたい**: Python、R、Julia（簡潔に書け、豊富な数値計算ライブラリがある）

大工道具にノコギリ、金づち、かんながあるように、**目的に最も適した道具（言語）を選ぶ**ことが重要です。

### Pythonの特徴
本科目ではプログラミング言語として **Python（パイソン）** を使用します。Pythonには以下のような大きな特徴があります：

1. **シンプルで読みやすい文法**:
   コードのブロック（まとまり）を `{ }` ではなく**インデント（字下げ）**で表すため、誰が書いても読みやすく統一感のあるコードになります。
2. **インタプリタ型言語**:
   コンパイル（事前の一括機械語変換）を必要とせず、プログラムを1行ずつ解釈しながら対話的に実行できます。試行錯誤が容易で、初学者の学習やデータ分析に最適です。
3. **強力なライブラリ群とエコシステム**:
   AI、深層学習、強化学習（PyTorch、TensorFlow、Gymnasiumなど）、科学技術計算（NumPy、SciPy）、データ分析（pandas、matplotlib）の世界標準ツールが揃っています。

---

## Python開発環境の導入

本科目では、受講生各自のPCローカル環境にPythonの**公式標準インストーラ**を用いて環境を構築します。

::: {.callout-warning}
### 注意：すでにPythonがインストールされている場合
Anaconda、Homebrew、過去のバージョンなど、すでにPC内に別のPython環境が存在している場合、環境変数の競合やパッケージ導入時のトラブルの原因になることがあります。不要な古いバージョンや重複する環境がある場合は、事前にアンインストールするか、どのPythonが呼び出されているかを把握しておく必要があります。

ターミナルで `python --version` や `which python`（macOS）、`where python`（Windows）を実行して、現在どのPythonが認識されているかテスト・確認しましょう。
:::

### ターミナルの使い方
プログラミングやAI開発では、画面上のボタンをクリックするGUI（Graphical User Interface）だけでなく、文字コマンドを入力して指示を出す**CUI（Command Line Interface / ターミナル）**を多用します。

- **Windows**: 「PowerShell」または「ターミナル」アプリを開きます。
- **macOS**: 「ターミナル」アプリ（Applications → Utilities → Terminal）を開きます。

ターミナルでは、キーボードからコマンドを入力して Enter を押すことで、OSに対して直接操作を指示します。

### 標準インストーラでローカルに導入する手順

#### ステップ1: 公式サイトからダウンロード
1. ブラウザで Python 公式サイト（[https://www.python.org/](https://www.python.org/)）にアクセスします。
2. ナビゲーションメニューの「**Downloads**」から、使用しているOS向けの最新安定版インストーラをダウンロードします。

#### ステップ2: インストールの実行
- **Windows の場合**:
  - ダウンロードした `.exe` ファイルを起動します。
  - **最重要**: インストール画面の下部にある **「Add python.exe to PATH」（PATHにPythonを追加する）に必ずチェックを入れてください**。これにチェックを入れないと、ターミナルから `python` コマンドが見つからなくなります。
  - 「**Install Now**」をクリックして進めます。
- **macOS の場合**:
  - ダウンロードした `.pkg` ファイルを起動し、指示に従って「続ける」をクリックしてインストールを完了させます。

#### ステップ3: 導入確認
ターミナルを開き、以下のコマンドを入力してバージョン番号が表示されるか確認します：

```bash
# Windows または環境に応じたコマンド
python --version

# macOS の場合（python3 と入力する場合があります）
python3 --version
```

`Python 3.x.x` のようにバージョン情報が表示されれば、導入は成功です。

---

## Pythonプログラムの実行

Pythonプログラムを実行するには、大きく分けて「REPLで対話的に実行する方法」と「スクリプトファイルを作成して実行する方法」の2つがあります。

### 対話型シェル（REPL）
REPL（Read-Eval-Print Loop）は、入力したコードをその場で1行ずつ評価・実行して結果を返す対話型の実行環境です。

ターミナルで `python`（または `python3`）と打ち込んで Enter を押すと、`>>>` というプロンプトが表示され、REPLが起動します。

```python
>>> print("Hello, Python!")
Hello, Python!
>>> 123 + 456
579
```

REPLを終了したいときは、`exit()` と入力して Enter を押すか、以下のショートカットキーを押します：
- **Windows**: Ctrl + Z を押した後に Enter
- **macOS**: Ctrl + D

REPLは、関数の挙動をちょっと確かめたいときや、簡単な計算を試したいときに非常に便利です。

### スクリプトファイルの作成と実行
まとまったプログラムを作成する場合は、プログラムをテキストファイル（拡張子 `.py`）に保存し、それをまとめて実行します。

#### VS Code（Visual Studio Code）の使い方
本講義では、プログラム開発用エディタとして Microsoft 社が提供する **Visual Studio Code（VS Code）** の利用を推奨します。

1. **フォルダを開く**: VS Codeを起動し、「ファイル」→「フォルダーを開く」から、講義用の作業フォルダを選択します。
2. **ファイルの新規作成**: ファイル作成アイコンをクリックし、ファイル名を `hello.py` とします。
3. **拡張機能（Python Extension）の導入**: 初回起動時に右下に表示されるPython拡張機能のインストール案内、または左側の拡張機能アイコンから「Python」拡張機能をインストールします。

`hello.py` に以下のコードを記述して保存（Ctrl + S / Cmd + S）してみましょう：

```python
# hello.py
print("Hello, World!")
print("コンピュータと強化学習の世界へようこそ！")
```

#### ターミナルからスクリプトを実行する
VS Codeの下部パネルにある「ターミナル（Terminal）」を開くか、PCのターミナルでファイルが保存されているフォルダへ移動し、以下のコマンドを実行します：

```bash
# スクリプトの実行
python hello.py

# macOS で python3 コマンドを使う場合
python3 hello.py
```

画面に指定したメッセージが表示されれば、スクリプト実行は成功です！

---

## Python言語の文法

ここからは、Python言語の基本的な文法のルールを見ていきましょう。

### print関数と計算式
`print()` のカッコの中に計算式を書くと、コンピュータがその計算を行い、結果を表示します。

```python
print(1 + 2)
print(10 - 3)
print(4 * 5)
print(20 / 4)
```

四則演算には以下の記号（演算子）を使用します：
- 足し算: `+`
- 引き算: `-`
- 掛け算: `*`
- 割り算: `/`
- 割り算の商（整数部分）: `//`
- 割り算の余り（剰余）: `%`
- べき乗: `**`

::: {.callout-note icon=false}
### 練習問題 01-03：1年間の秒数を計算する
`print()` 関数と四則演算を用いて、1年間（平年＝365日）が何秒であるかを計算して表示するプログラムを作成してください。
:::

### 実行の順序
プログラムは複数の「文（statement）」から成り立っています。
特に指定（条件分岐やループ、関数呼び出しなど）がなければ、**上から下へ書かれた順に1行ずつ実行**されます。

```python
print("1番目に実行されます")
print("2番目に実行されます")
print("3番目に実行されます")
```

### 空白
Pythonでは、空白文字（スペース）の使い方に重要なルールがあります。

- **先頭の空白（インデント）には意味がある**:
  行の先頭にスペースを置くこと（字下げ・インデント）は、Pythonではプログラムの構造（ブロック）を表す文法的な意味を持ちます。理由なく行の先頭にスペースを入れると、`IndentationError` というエラーになります。
- **全角空白の混入に注意**:
  プログラムのコード中（文字列のクォーテーションの外側）に全角空白（日本語入力のスペース）が含まれていると、文法エラー（`SyntaxError`）の原因になります。

::: {.callout-note icon=false}
### 練習問題 01-04：日本語文字の空白（全角空白）を入れてみる
コード中の行頭や記号の間に、わざと日本語文字の空白（いわゆる全角空白）を入れてプログラムを実行し、どのようなエラーメッセージが表示されるか確認してください。
:::

### コメント
`#` 記号から行末までは**コメント**となり、コンピュータは実行時にこれを完全に無視します。
コードの意図や注意点を人間のために書き残す際に使用します。

```python
# ここはコメントです。実行には影響しません。
print("コメントの動作確認")  # 行の途中からコメントにすることもできます
```

### 変数
**変数**とは、計算結果やデータを一時的に保存しておくための「名前の付いた箱」のようなものです。

### 代入文
変数にデータを入れる操作を**代入**と呼び、等号記号 `=` を使って記述します。
プログラミングにおける `=` は、数学の「等しい」ではなく、**「右辺の計算結果を左辺の変数に入れる」**という意味です。

```python
x = 10
print(x)

x = 20  # 変数の値を上書き（再代入）
print(x)
```

::: {.callout-note icon=false}
### 練習問題 01-05：複数の代入文で変数値の変更を確認する
変数 `count` に初期値 `5` を代入して表示し、次に `count = count + 3` を実行して表示し、さらに `count = count * 2` を実行して表示するプログラムを作成し、複数の代入文で変数の値が変更される様子を確認してください。
:::

### 関数呼び出し
**関数**とは、特定の処理をまとめて名付けたものです。関数名の後ろにカッコ `()` を付け、必要に応じて引数（ひきすう）を渡すことで、その機能を呼び出します。
すでに使っている `print()` も代表的な組み込み関数です。また、文字列やデータの長さを調べる `len()` 関数などもあります。

```python
name = "OpenAI"
print(len(name))  # 文字列の長さを表示（6）
```

::: {.callout-note icon=false}
### 練習問題 01-06：lenで文字列の長さを表示する
`len()` 関数を用いて、文字列 `"Reinforcement Learning"` の長さを表示するプログラムを作成してください。
:::

### 関数定義
既存の関数を使うだけでなく、自分で新しい関数を定義することもできます。
関数を定義するには `def` キーワードを使い、以下のように記述します：

```python
def greet(user_name):
    print("こんにちは、" + user_name + "さん！")

# 関数の呼び出し
greet("太郎")
greet("花子")
```

::: {.callout-note icon=false}
### 練習問題 01-07：挨拶関数の定義
文字列を引数として受け取り、「Hello 〇〇」と表示する関数を作成し、呼び出して動作を確認してください。
:::

### モジュール

#### モジュールとは
Pythonには、特定の目的のために作成された便利な関数や定数のまとまりである**「モジュール（Module）」**や、複数のモジュールを束ねたパッケージ・ライブラリが豊富に用意されています。すべてを自分でゼロから作成しなくても、標準モジュールや世界中で公開されているライブラリを活用することで、高度な処理を簡単に組み立てることができます。

#### import文の使い方
モジュールを利用するには、`import` 文を使ってプログラム内に読み込みます。モジュール内の関数や定数を使うときは、`モジュール名.関数名()` や `モジュール名.変数名` のようにドット `.` で繋いで呼び出します。

```python
import math

# 平方根の計算
print(math.sqrt(16))  # 4.0

# 円周率
print(math.pi)        # 3.141592653589793
```

---

## まとめ

- 高校「情報I」で学んだ順次・分岐・反復は、あらゆる高水準プログラムの基礎をなす。
- コンピュータの5大機能のうち、CPUが演算と制御を担い、主記憶から命令を1個ずつ読み込んで実行する。
- CPUが直接読めるのは機械語（0と1の並び）だけであり、人間が扱いやすいように高水準言語（Pythonなど）が生まれた。
- 公式標準インストーラを用いてPCローカル環境にPythonを導入し、ターミナルおよびVS Codeを用いたREPL・スクリプト実行の基本操作を習得した。
- Python言語の基本文法として、`print()`、代入文と変数、空白の意味、関数定義、モジュールのインポートを学んだ。
